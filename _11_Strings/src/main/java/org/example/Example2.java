package org.example;

public class Example2 {

    // What is concatination?
    // combine with string with the help of '+'
    // it is the combination of strings



    public static void main(String[] args) {
        String str = "Chetan";
                 //  "012345"



       //  str.length();  no. of character of string / size of string
       //  str.equals(str2) -----> check the value not address/reference/object
        // str.charAt(int i)  ----> get char by index
        // str.substring(i) ------> Return the substring from the ith index character to end
        // str.substring(i,j) ------>  Returns the substring from i to j-1 index
        // str.indexOf(String s) ------>Returns the index within the string of the first occurrence of
        //the specified string.
        // str.trim()

        System.out.println(str + "Singh");








        System.out.println(str.substring(2,4));







    }
}
